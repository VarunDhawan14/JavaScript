// ----- Basic Use of Functions ------

// function sum(x, y) {
//   add = x + y;
//   console.log("Value after addition : ");
//   return add;
// }

// const value = sum(20, 30);
// console.log(value);

// --- use of arrow functions in javascript -----

const arrowsum = (a, b) => {
  console.log(a + b);
};
arrowsum(3, 4);
